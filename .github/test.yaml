name: Test
on:
  push:
    branches:
      - "**"
    tags-ignore:
      - "[0-9]+.[0-9]+.[0-9]+"
  pull_request:
    branches:
      - "**"
jobs:
  test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup go
        uses: actions/setup-go@v1
        with:
          go-version: 1.16

      - name: Install dependency
        run: go get -v -d

      - name: Test
        run: |
          go test -v .
